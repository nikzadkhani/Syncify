import { Route } from '@angular/router';

import { ViewPlaylistComponent } from './view-playlist.component';

export const viewPlaylistRoute: Route = {
  path: 'viewPlaylist',
  component: ViewPlaylistComponent
};
