import { Route } from '@angular/router';

import { CreatePlaylistComponent } from './create-playlist.component';

export const createPlaylistRoute: Route = {
  path: 'createPlaylist',
  component: CreatePlaylistComponent
};
